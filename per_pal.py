"""
    Write an efficient function that checks 
    whether any permutation ↴ of an input string is a palindrome. 
    
    >>> has_palindrome_permutation('civic')
    True 

    >>> has_palindrome_permutation('ivicc')
    True

    >>> has_palindrome_permutation('civil')
    False

    >>> has_palindrome_permutation('livci')
    False 

"""

def has_palindrome_permutation(the_string):


















if __name__ == '__main__':
    import doctest
    if doctest.testmod().failed == 0:
        print("\n*** ALL TESTS PASSED. HOORAY!\n")